import type {
  FormInstance,
  FormItemProps,
  FormListFieldData,
  FormListOperation,
} from 'antd';
import { Form as AntForm } from 'antd';
import type {
  ErrorListProps,
  FormListProps,
  Rule,
  RuleObject,
  RuleRender,
} from 'antd/es/form';

export type {
  ErrorListProps,
  FormInstance,
  FormItemProps,
  FormListFieldData,
  FormListOperation,
  FormListProps,
  Rule,
  RuleObject,
  RuleRender,
};

export const Form = AntForm;
